# Tests

Planned test layout:

- `e2e/`: Playwright or Cypress tests for browser flows.
- `integration/`: API and frontend/backend integration tests.

The frontend does not include tests yet. Add smoke tests once the team finalizes the core user flows.
