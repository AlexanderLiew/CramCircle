# Shared Package

Use this package for code shared between frontend and backend:

- TypeScript DTOs and API response types.
- Validation schemas.
- Route constants.
- Feature flags or shared enums.

Keep it framework-neutral so both apps can import it safely.
