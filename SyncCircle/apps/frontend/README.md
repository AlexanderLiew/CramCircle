
# SyncCircle frontend prototype

This is the SyncCircle frontend prototype for a student collaboration web app.

## Running the code

Run `corepack pnpm install` from the repository root to install dependencies.

Run `corepack pnpm dev` from the repository root to start the development server.
  
